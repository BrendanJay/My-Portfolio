# The current portfolio does not require environment variables.
# VITE_* values are public in the browser bundle; never put secrets here.
# Optional example for a future public client-side integration:
VITE_PUBLIC_API_KEY=replace-with-a-public-anonymous-key